#include "measure_diode.h"

/*
 * T-1.3.1-R1:
 * Diode v1 is intentionally header-inline (same pattern as measure_cont)
 * to avoid touching managed-build source lists in this scoped feature branch.
 */
