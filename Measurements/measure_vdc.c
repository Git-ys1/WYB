#include "measure_vdc.h"

/*
 * T-1.4.0-R1:
 * VDC logic is intentionally header-inline (same pattern as measure_cont/diode)
 * to avoid touching managed-build source lists in this scoped feature branch.
 */
