# T-0.1.9 运行报告（修订版执行结果）

生成时间：2026-03-02
工程路径：`F:\Project\stm32G474VETx\study\wanyongbiao\wanyongbiao`

## 1. 总体结论

本轮已完成 T-0.1.9 对接改造，代码层面已落地 0.1.8 四项最小里程碑所需骨架和接口联通。  
当前阻塞项是本终端缺少本地编译工具链（`arm-none-eabi-gcc/cmake/make/ninja` 未在 PATH），因此未在此终端产出可执行二进制；请在 STM32CubeIDE 内执行一次 Build + 烧录联测。

## 2. 你要求的 8 条修订落实状态

1. Keep User Code：已落实（`wanyongbiao.ioc` 为 `ProjectManager.KeepUserCode=true`）。
2. TIM2 Clock Source=Internal：已落实（`MX_TIM2_Init()` 与 `.ioc` 均为 Internal）。
3. `tim2_clk_hz` 与 `tick->us`：已落实（BSP 内按 APB1 分频计算时钟，并做 ticks 到 us 换算）。
4. 蜂鸣 GPIO 兜底方波：已落实（PWM失败自动切软件方波翻转 PB8）。
5. PB4/PB6 与调试冲突：已落实（`.ioc` 为 `SYS.Debug=Serial Wire`）。
6. I2C 100k + 3.3V 上拉：已落实（I2C2/I2C3 初始化为 100k 对应 timing；文档明确上拉到 3.3V）。
7. Drivers->UserDrivers 迁移可选：已落实（本轮未迁移目录，保留现状）。
8. BSP_USE_HAL_PORT 开关位置：已落实（`BSP/bsp_config.h` 默认开启，且支持预处理宏覆盖）。

## 3. 已完成的关键对接

## 3.1 工程入口与主循环
- 唯一 MCU 入口为 `Core/Src/main.c`。
- 根目录 `main.c` 已改为 `main_host.c` 且入口函数改名，避免 duplicate `main`。
- 主循环已固定为：
```c
while (1) {
    app_poll_button();
    app_measure_tick();
    app_ui_tick();
    app_beep_tick();
}
```

## 3.2 外设映射
- I2C2/PC4+PA8：OLED
- I2C3/PC8+PC9：ADS1110
- TIM2_CH1/PA0：输入捕获（频率/占空比）
- TIM16_CH1/PB8：蜂鸣 PWM
- PC13 EXTI：按键
- PB4/5/6、PD5/6/7、PB13/14：三组 CD4051 地址控制

## 3.3 BSP HAL 端口
- `BSP/bsp_port_hal_template.c` 已完成 HAL 映射实现，包含 I2C/GPIO/Delay/PWM/IC/EXTI。
- `BSP/bsp_port_stub.c` 与 `BSP_USE_HAL_PORT` 互斥编译逻辑已修正。
- TIM2 捕获缓存通过 `HAL_TIM_IC_CaptureCallback()` 更新，`bsp_freq_get_capture()` 对上层输出 `period_us/high_us`。

## 3.4 CubeMX 侧补齐
- 已补充 I2C2/I2C3/TIM16 到工程初始化链。
- 已添加 I2C MSP 与 TIM16 MSP/PostInit。
- 已启用 `HAL_I2C_MODULE_ENABLED`。
- `.ioc` 已切换 `ProjectManager.TargetToolchain=STM32CubeIDE`。

## 4. 里程碑覆盖情况（代码层）

1) OLED 点亮并菜单切换：已具备（OLED 初始化 + app UI 页面 + 按键短/长按状态机）。  
2) ADS1110 raw/mV 显示：已具备（ADS 驱动 + UI 显示链路）。  
3) TIM2 频率/占空比显示：已具备（IC 捕获 + 平滑 + UI）。  
4) MUX 切换 ADS 联动：已具备（测量模块调用 `mux_set_*` 并读取 ADS）。

> 注：以上为“代码接入已具备”；硬件实测以 `docs/validation_checklist.md` 填表结果为准。

## 5. 本轮未完成项（客观限制）

1. 未在此终端直接编译（本机缺少命令行编译工具链）。  
2. 未执行上板实测（需你在 CubeIDE 下载后按 T-I2C2/T-I2C3/T-IC/T-MUX/T-M1 逐项回填）。

## 6. 你现在只需做的操作

1. 在 CubeIDE 打开该工程并从 `.ioc` 重新 Generate Code（保持 Keep User Code）。  
2. 直接 Build 工程；若报错，把前 30 行错误粘给我，我会按文件逐条修。  
3. 上电后按 `docs/validation_checklist.md` 记录 T-I2C2/T-I2C3/T-IC/T-MUX/T-M1 实测值。

