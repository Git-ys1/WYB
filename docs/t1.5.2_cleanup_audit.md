# T-1.5.2-R1 Cleanup Audit (No Deletions This Round)

## Audit Rule

- 先判断是否参与当前构建与运行。
- 本轮只输出分类：`Retain / Rename / Delete-Later`。
- 真删代码放到后续单独清理任务。

## Retain / Rename / Delete-Later

| Item | Current Build/Run | Decision | Rationale / Replacement |
|---|---|---|---|
| `Drivers/drv_beep.c` (`PB1`, active-low backend) | Build+Run | `Retain` | 当前真实蜂鸣器运行链路；与 `App/app.c` 的 `beep_*` 调用一致。 |
| `BSP/bsp_port_hal_template.c` 的 `PB1` GPIO beep 控制 | Build+Run | `Retain` | BSP pin map 已绑定 `BSP_PIN_BEEP -> PB1`。 |
| `App/app.c` 中 `MUX_MODE_AC` 旧命名 | Build+Run | `Rename` | 语义落后；统一升级为 `MUX_MODE_FREQ`，保留一轮兼容别名。 |
| `BSP/bsp_keys.c` 四键扫描（OK/LEFT/RIGHT/BACK） | Build+Run | `Retain (dev-only note)` | 正式路径消费 `LEFT/RIGHT`，`OK/BACK` 为 dev-only；先保留避免输入链回归。 |
| `Core`/HAL 的 `TIM16 + PB8 (BEEP_Pin)` 链 | Build（Run未走主链） | `Delete-Later` | 当前运行蜂鸣器已走 `PB1 GPIO`；PB8/TIM16 属历史链，需单独清理任务验证后删除。 |
| `bsp_port_hal_template.c` 中 software PWM fallback | Build（默认分支未用） | `Delete-Later` | Active backend 固定启用；fallback 仅作兜底，后续可删除并同步 `drv_beep`。 |
| README 中历史/冲突口径段落 | Doc only | `Retain +逐步收敛` | 本轮已补 `T-1.5.2` 真相冻结条目，后续可再做文档瘦身。 |

## Evidence Focus for This Round

- FREQ 入口：`App/app.c` 使用 `mux_set_mode(MUX_MODE_FREQ)`。
- MODE 物理映射：`Drivers/drv_mux4051.c` 的 `mode_to_phys_ch()`。
- FREQ 捕获链：`BSP/bsp_port_hal_template.c` + `Drivers/drv_freq_ic.c`。
- Debug 证据：`App/app.c` FREQ debug 页显示 `MODE/P/H/CLK/IRQ/CCR/INV/F/D/ERR`。
